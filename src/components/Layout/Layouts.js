import React from 'react';
import Aux from '../../hoc/Aux'; 

const Layout = () => (
    <Aux>
    <div>Toolbar, SideDrawer, Backdrop</div>
    <main>
        {response.children}
    </main>
    </Aux>
)